#!/bin/sh
set -e
# Remove apenas os artefatos gerados; preserva /etc/asterisk-push-notify/push.env
# (configuracao do usuario) e /etc/asterisk-push-notify/devices.json (tokens).
echo "[asterisk-push-notify] removido. /etc/asterisk-push-notify/push.env e devices.json foram preservados."
exit 0
